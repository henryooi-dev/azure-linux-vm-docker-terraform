# azure-linux-vm-docker-terraform
Terraform scripts to provision an Azure Linux Virtual Machine with Docker installed, including networking, security groups, and public IP configuration. Supports Windows, Linux, and macOS hosts for SSH automation.
